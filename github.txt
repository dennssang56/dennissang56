Create a reapository on github account
Check wheather yo do have git(library) installed and configure locally
Initialize git instance within the project we'd want transport
Add our codebase to the git transporter pipeline
Commid the codebase that was added
Create a connection between the local environment with the remote environment
Push the codebade to the remote environment

***Branching, pull request and merging 